"""Capacity and distortion harness.

Not part of the objstego package -- this measures the tool rather than being
part of it, and unlike `src/objstego/` it may use third-party packages.
Install them with `pip install -e ".[analysis]"`.
"""
